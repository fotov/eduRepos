
&НаСервере
Процедура ЗаполнитьНаСервере()
	ОбработчикиОбновления = ПланыВидовХарактеристик.НастройкиПрограммы.ПолучитьОбработчикиОбновления();
	Для Каждого Эл Из ОбработчикиОбновления Цикл
		ПланыВидовХарактеристик.НастройкиПрограммы.ВыполнитьОбработчикОбновления(Эл, "");
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	ЗаполнитьНаСервере();
	Элементы.Список.Обновить();
КонецПроцедуры
